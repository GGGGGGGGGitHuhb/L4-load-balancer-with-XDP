# 当前概况

当前项目处于初始化阶段，尚未进入 `V0.1 / S1` 实现。已完成的主要工作是明确项目方向、根文档结构、系统架构边界、版本路线和三类角色 Agent 的职责分工。

当前最近完成内容：

- `README.md` 已记录项目定位、当前状态、环境要求和文档索引。
- `ARCHITECTURE.md` 已记录 C++ 用户态 L4 负载均衡器、控制面、用户态数据面和 XDP/eBPF 数据面的长期边界。
- `ROADMAP.md` 已记录从 `V0.1` 用户态 TCP 转发到 `V1.2` XDP L4 fast path 原型的总体路线。
- `AGENTS.md` 和三个角色目录下的 `AGENTS.md` 已记录仓库级与角色级协作规则。

下一阶段准备进入：

- `V0.1 / S1 工程骨架与配置入口`

下一阶段开始前必须注意：

- 当前仓库没有 C++ 工程骨架，README 中的 CMake/Ninja 命令仍是目标形态，不能被当作已可用命令。
- `docs/specs/`、`docs/runbooks/`、`docs/benchmarks/` 目录尚未创建，相关长期文档应随着阶段推进逐步生长。
- XDP/eBPF 真实验证环境尚未确认，不能把 WSL2 当作 XDP native mode 性能验证环境。

当前不存在阻塞 `V0.1 / S1` 启动的技术债。

## 阶段状态

### V0.1 / S1 工程骨架与配置入口

状态：未开始。

原计划目标：

建立 CMake 工程、目录结构、CLI 入口、配置模型和最小测试框架，为后续 TCP 转发实现提供可构建、可测试的基础。

已完成内容：

- 尚未开始实现。

待处理内容：

- 创建 C++20 工程骨架。
- 建立 `src/`、`tests/` 和必要的 CMake 结构。
- 确认 C++ 测试框架选择。
- 建立最小 CLI 和配置模型。
- 创建或更新最小配置规格文档。

验证情况：

- 尚无 Builder 报告。
- 尚无 Reviewer 报告。
- 尚无可运行测试。

相关文档：

- 设计文档：`docs/leader/designs/V0.1/S1-design.md`
- 实现报告：`docs/builder/reports/V0.1/`
- 审查报告：`docs/reviewer/reports/V0.1/`

### V0.1 / S2 TCP 转发最小闭环

状态：未开始。

原计划目标：

实现 TCP 监听、后端连接、双向转发、连接关闭和基础日志。

已完成内容：

- 尚未开始实现。

待处理内容：

- 等待 `V0.1 / S1` 完成后启动。

验证情况：

- 尚无 Builder 报告。
- 尚无 Reviewer 报告。

相关文档：

- 设计文档：`docs/leader/designs/V0.1/S2-design.md`
- 实现报告：`docs/builder/reports/V0.1/`
- 审查报告：`docs/reviewer/reports/V0.1/`

### V0.1 / S3 TCP 转发验证与报告

状态：未开始。

原计划目标：

补充本地 echo 后端验证、集成测试、最小 README 运行命令和阶段报告。

已完成内容：

- 尚未开始实现。

待处理内容：

- 等待 `V0.1 / S2` 完成后启动。

验证情况：

- 尚无 Builder 报告。
- 尚无 Reviewer 报告。

相关文档：

- 设计文档：`docs/leader/designs/V0.1/S3-design.md`
- 实现报告：`docs/builder/reports/V0.1/`
- 审查报告：`docs/reviewer/reports/V0.1/`

## 技术债条目

### TD-001 README 构建命令尚未真实可用

状态：待处理。

影响范围：`README.md`、`V0.1 / S1`、构建与测试入口。

发现来源：初始化文档编写。

问题描述：

`README.md` 中已经列出目标工程形态下的 CMake/Ninja 构建命令和 CTest 测试命令，但当前仓库尚未包含 `CMakeLists.txt`、源码目录和测试目标。这些命令已经标注为后续工程骨架建立后使用，但在 `V0.1 / S1` 完成前仍不能真实运行。

风险：

如果后续实现忘记同步 README，用户或新 Agent 可能复制命令后得到失败结果，误判项目不可构建或文档不可信。

处理建议：

在 `V0.1 / S1` 建立 CMake 工程骨架后立即更新 README，将目标命令改为真实可运行命令，并补充预期输出。

当前决定：

接受短期初始化状态，要求 `V0.1 / S1` 完成时处理。

相关证据：

- `README.md`
- `ROADMAP.md` 中 `V0.1 / S1 工程骨架与配置入口`

更新记录：

- `2026-05-19`：新增，原因是 README 已给出目标构建命令，但工程骨架尚未落地。

### TD-002 配置格式和测试框架尚未确定

状态：待处理。

影响范围：`V0.1 / S1`、`src/config/`、`tests/`、`docs/specs/config-schema.md`。

发现来源：架构文档和路线图初始化。

问题描述：

项目已经确定需要静态配置文件、配置校验和自动化测试，但尚未确定配置格式和 C++ 测试框架。候选配置格式可能包括 JSON、YAML 或 TOML；测试框架可能包括 GoogleTest、Catch2 或其他轻量方案。

风险：

如果在实现中临时选择且不记录原因，后续配置兼容性、依赖管理和测试组织会变得随意，影响项目可维护性和简历叙事的一致性。

处理建议：

在 `V0.1 / S1` 阶段设计中明确选择标准，并在实现报告中记录最终选择原因。配置格式一旦进入 README 或 `docs/specs/config-schema.md`，后续变更应按兼容性原则处理。

当前决定：

等待 `V0.1 / S1` 阶段设计决定。

相关证据：

- `ARCHITECTURE.md` 中项目技术概览和数据模型与持久化章节。
- `ROADMAP.md` 中 `V0.1 / S1 工程骨架与配置入口`。

更新记录：

- `2026-05-19`：新增，原因是配置格式和测试框架是工程骨架阶段的关键基础选择。

### TD-003 XDP/eBPF 真实验证环境尚未确认

状态：持续跟踪。

影响范围：`V1.1`、`V1.2`、XDP/eBPF 数据面、性能验证。

发现来源：用户环境讨论和架构文档初始化。

问题描述：

当前开发环境是 WSL2，适合用户态 C++ 开发、基础测试和部分 eBPF 编译学习，但不适合作为 XDP native mode 的最终性能验证环境。项目后续需要确认 Linux 裸机、双系统、专用 Linux 机器或合适虚拟化环境。

风险：

如果没有提前确认环境，`V1.1` 和 `V1.2` 可能只能完成编译或 generic mode 验证，无法给出可信的 XDP attach、driver mode 或性能对比结果。

处理建议：

在进入 XDP/eBPF 相关版本前，创建 `docs/runbooks/linux-xdp-env.md`，记录目标内核、网卡、权限、工具链、attach mode 和验证命令。若无法获得合适环境，应把性能验证范围收窄并明确写入技术债。

当前决定：

持续跟踪，不阻塞 `V0.1` 到 `V1.0` 的用户态版本。

相关证据：

- `README.md` 环境要求。
- `ARCHITECTURE.md` XDP/eBPF 数据面层。
- `ROADMAP.md` 中 `V1.1` 和 `V1.2`。

更新记录：

- `2026-05-19`：新增，原因是当前环境为 WSL2，XDP 真实验证需要后续确认。

### TD-004 长期规格文档目录尚未创建

状态：接受延期。

影响范围：`docs/specs/`、`docs/runbooks/`、`docs/benchmarks/`、阶段设计与验收。

发现来源：用户关于 API 文档、数据库设计文档替代物的讨论。

问题描述：

项目已经明确后续需要网络语义、配置 schema、UDP flow table、XDP map schema、运行手册和性能方法等长期文档，但当前仓库尚未创建这些目录和文档。这是符合项目阶段的，因为代码和行为尚未落地，不适合提前写完整规格。

风险：

如果后续阶段只写代码不补充规格文档，项目会缺少高性能网络方向最重要的设计证据：数据路径、状态模型、包处理边界和性能验证方法。

处理建议：

按阶段自然生长：

- `V0.1` 创建 `docs/specs/config-schema.md` 和 `docs/specs/tcp-forwarding-semantics.md`。
- `V0.2` 创建 `docs/specs/udp-flow-table.md` 和 `docs/specs/scheduler.md`。
- `V0.3` 创建 `docs/specs/health-check.md` 和 `docs/specs/metrics.md`。
- `V0.4` 创建 `docs/benchmarks/methodology.md`。
- `V1.1` 或 `V1.2` 创建 `docs/specs/xdp-map-schema.md` 和 `docs/runbooks/linux-xdp-env.md`。

当前决定：

接受延期。Leader 在阶段设计中负责指定最小文档产出，Builder 随实现更新，Reviewer 审查一致性。

相关证据：

- `ROADMAP.md` 相关文档章节。
- `docs/leader/AGENTS.md` 规格文档规划职责。
- `docs/builder/AGENTS.md` 规格文档维护职责。
- `docs/reviewer/AGENTS.md` 规格文档审查职责。

更新记录：

- `2026-05-19`：新增，原因是长期规格文档需要随项目演进生成，当前不应一次性铺满。

## 风险观察

### OBS-001 文档先行可能导致早期文档多于实现

观察到什么：

当前项目已经建立 README、架构、路线图和 Agent 规则，但尚未进入实现阶段。

目前为什么还不算技术债：

这是项目初始化的预期状态。文档用于约束后续实现范围，并未阻塞开发。

什么时候需要升级为技术债：

如果 `V0.1 / S1` 完成后 README、ROADMAP、ARCHITECTURE 与实际工程结构仍明显不一致，或文档承诺的命令长期不可运行，应升级为技术债。

### OBS-002 XDP 与完整 TCP proxy 的边界需要持续防守

观察到什么：

项目目标包含 L4LB 和 XDP/eBPF，容易在后续讨论中把 XDP fast path 扩张成完整 TCP proxy。

目前为什么还不算技术债：

`ARCHITECTURE.md` 和 `ROADMAP.md` 已明确 XDP 不承载完整 TCP 代理语义。

什么时候需要升级为技术债：

如果阶段设计或实现开始把完整 TCP 连接代理逻辑塞入 XDP/eBPF，应新增技术债或要求返工。

### OBS-003 高性能目标需要可复现性能证据

观察到什么：

项目面向高性能网络方向，但当前尚未有 benchmark 方法、工具或结果。

目前为什么还不算技术债：

性能验证在 `V0.4` 才进入路线图，不要求初始化阶段完成。

什么时候需要升级为技术债：

如果 `V0.4` 后仍只有功能测试，没有可复现的吞吐、延迟或 CPU 使用率记录，应升级为技术债。

## 下一阶段检查点

进入 `V0.1 / S1 工程骨架与配置入口` 前，Leader 应重点检查：

- 确认阶段设计不提前实现 TCP 转发细节，除非是 CLI 或配置验证所需的最小占位。
- 确认 CMake/Ninja/LLVM 命令在当前 WSL2 环境可运行，或明确无法运行的原因。
- 确认配置格式和测试框架选择有理由，并记录到设计或报告中。
- 确认 README 中的命令在工程骨架落地后同步更新。
- 确认 `docs/specs/config-schema.md` 是否需要在本阶段创建最小版本。
- 确认生成文件、构建产物和测试临时文件不会进入源码或手写文档目录。

## 关闭规则

技术债关闭时不删除条目。关闭方式：

- 将状态改为 `已解决` 或 `不再适用`。
- 在“更新记录”中说明关闭日期和依据。
- 引用 Reviewer 审查报告、测试命令、设计变更或用户明确决定。
- 如关闭后仍有残余风险，应新增风险观察或新的技术债条目。

可接受的关闭依据：

- Reviewer 审查报告确认通过。
- 测试命令和结果证明问题已修复。
- 阶段设计文档明确取消该需求。
- 用户明确接受风险或延期。

## 变更记录

- `2026-05-19`：创建初版技术债跟踪文档，记录初始化阶段的构建命令、配置选择、XDP 环境和长期规格文档风险。
