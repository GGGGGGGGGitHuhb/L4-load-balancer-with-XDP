# V0.3 版本验收矩阵

基线 V0.3-S3-D1 Approved。2026-09-09 Builder001、Reviewer001独立PASS及Leader003最终同步齐备，S3与V0.3开发范围Completed，六条完成标准均满足。继承S1 Builder001/Reviewer001 PASS/Leader003，以及S2 Builder002/Reviewer002 PASS/Leader003；S2两项返工关闭不等于S3已验收。

公开执行入口：[本机运行手册](../runbooks/local-v0.3-validation.md)。新场景命令 `bash tests/v03_validate.sh <PRODUCT> <EVIDENCE_ROOT>`；单项 `python3 tests/v03_system_test.py <PRODUCT> <EVIDENCE_ROOT> tcp|udp|failure`。网络命令在隔离且lo已启用的namespace中执行。期望值由nonce/backend账本独立计算，产品metrics仅为实际值。

| 标准 | 要求与继承输入 | S3新增命令/证据 | 最终结论及边界 |
|---|---|---|---|
| V03-1 | 不健康后端不获新会话/flow，旧绑定不迁移；继承S1健康资格/旧token | runner的tcp/udp；actions、backend-events、ledger记录A坏后4个新B、全坏2次拒绝及旧A/B数据 | 通过；UDP只停TCP探活服务，UDP持续，不用ICMP重建冒充旧绑定 |
| V03-2 | 恢复重新参与；继承S1固定2成功/3失败及轮询 | tcp/udp先仅A恢复2个新A，再双恢复4次按当前cursor核身份 | 通过；恢复不重置cursor，不以两个都出现代替顺序 |
| V03-3 | 用户观察运行状态；继承S2字段口径与输出边界 | 三场景每阶段expected/actual快照账本；failure真实拒绝两次errors2；最终单final/active0 | 通过；成功提交bytes不保证应用消费，stderr同步风险不变 |
| V03-4 | 健康/指标自动化测试；继承原20项含S2异常drop/fd | CTest23项；工具负向 `python3 tests/v03_tool_negative.py <PRODUCT> <EVIDENCE_ROOT>` | Reviewer Debug快速22/22、Release完整23/23；Builder额外Debug expiry已披露，真实系统P1..P3与内部变异T1..T4分别记录 |
| V03-5 | 故障可复现 | 公开runner实际Release/Production执行，Release新增重复2次/并行2组/空格路径；ICMP各模式5次新namespace | 全通过；本轮10次采样及全套未复现，历史根因未定位，不能称性能/概率保证 |
| V03-6 | 最新Builder/Reviewer报告与LeaderClosing齐备 | Builder001（本地治理记录，不随clone分发）；Reviewer001 PASS（本地治理记录，不随clone分发）；Leader003完成报告（本地治理记录，不随clone分发） | 通过；本S3实现、独立审查及Leader收尾齐备，无未处置发现 |

原始Builder证据根为本工作区 `.stage-tmp/v0.3-s3/builder/`，可重新生成，不是普通clone运行的前置依赖。`final-Debug.log`/`final-Release.log`、`icmp/`、`extra-*.log`、`tool-negative/`、各run-*账本和cleanup为本次实际证据。Production不注册测试，普通uid38CLI及check-config静态追踪通过；详细命令、退出码和源码指纹见Builder报告。

Reviewer独立核对V03-1..5均通过：Debug快速22/22、Release完整23/23、Production公开三场景、38项普通uid CLI、Release新增重复2次/并行2组/空格路径、五路工具负向以及每模式5次新namespace ICMP采样通过；历史根因未定位。29个run（24正向/5故意负向）PID回收且fd相等，55指纹一致、生产/C++零差异。独立证据根 `.stage-tmp/v0.3-s3/reviewer/`，可由公开命令重新生成。V03-6本阶段Builder/Reviewer报告及Leader003收尾齐备；六标准全部完成。阶段完成不代表已提交、合并或发布，父按既有授权处理本地提交。
