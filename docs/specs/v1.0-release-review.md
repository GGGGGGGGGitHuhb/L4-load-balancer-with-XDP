# V1.0 用户态候选发布前审查

验证日期：2026-09-10。候选基线 `a1bfc4875373d196b60f32d149503de66a57a900`（V1.0/S2合并后的本地来源），分支 `codex/v1.0-s3`；S3只增加本记录并补充公开验收、技术债和状态文字。产品src、configs、CMake、全部测试/benchmark工具与基线一致，正式V0.4报告及数据字节保持。

## 当前结论与三种状态

- **开发范围Completed：已完成。** S1/S2/S3及六版本标准均满足，S3 Reviewer001独立PASS，Leader于2026-09-10完成收尾。
- **候选发布就绪：是。** 该结论只适用本记录界定的Linux/WSL用户态源码与配置/构建/工具，不表示生产级可靠性、安全认证、饱和容量或跨平台承诺。
- **实际发布：本阶段未执行。** 未授权或执行push/PR/merge/tag/Release/分发；完成后本地commit也不等于发布。不预填未来提交SHA。

[公开版本验收索引](v1.0-acceptance.md)映射ROADMAP六标准，[README](../../README.md)提供直接运行命令。完整角色manifest和原始日志为内部审计资料，不是普通clone必读入口。独立公开导出不是完整clone；只读本地Git源提供工具身份，recompute本身不需要产品build或Git历史。

## 被验证身份

产品、配置、CMake和测试工具的分组SHA256见下方身份清单。算法为：每组路径按字典序排列，对每文件计算SHA256，再将`<文件SHA256>  <相对路径>\n` UTF-8行串计算SHA256。分组不包含本文或其他Markdown，避免自哈希循环。完整文件manifest另行保留，最终状态文字更新不改变这组动态验证身份。

- 生产src：29文件，`b6f433f9164a3ecee0c4ad0f7cee65c43431623e0230d1e8563902cced781643`。
- 配置configs：1文件，`d8acbb4013e22f7db5414083a970cb7443f06bf1505d48e099943c77ba2074c1`。
- 构建CMake：1文件，`5f4eedba586ae06fa18394ac628f16940600bdfe277ea6f2ad1ebbe8ddfbdb5c`。
- 测试及工具tests：41文件，`a80683b3d27ad08c067ec0e17aa99ca3cf43418017b62e2f16784858f0ea6015`。

后续src/configs/CMake/关键测试工具变更须重新评估验证影响；纯收尾结果/链接文字由Leader核对身份和本地链接即可，不伪称重新运行动态集合。

## 环境与直接复核

Linux/WSL2回环、C++20/Clang++18.1.3、CMake3.28.3、Ninja1.11.1和Python3标准库；无下载、外网或外部服务。生产关闭BUILD_TESTING不依赖Python。受限执行器使用临时user/net namespace，仅启子lo，不改变宿主网络/代理/sysctl；普通Linux回环可直接运行，产品不要求root。namespace映射UID0不代替普通UID权限证据。

在新的普通源码目录或完整clone根执行，构建及输出目录不能复用旧验证结果：

```bash
mkdir -p .stage-tmp/release-check/tmp .stage-tmp/release-check/cache .stage-tmp/release-check/pycache
export TMPDIR="$PWD/.stage-tmp/release-check/tmp" TMP="$PWD/.stage-tmp/release-check/tmp" TEMP="$PWD/.stage-tmp/release-check/tmp"
export XDG_CACHE_HOME="$PWD/.stage-tmp/release-check/cache" PYTHONPYCACHEPREFIX="$PWD/.stage-tmp/release-check/pycache"
cmake -S . -B build -G Ninja -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug
cmake --build build -j4
ctest --test-dir build -N
./build/bin/l4lb --help
./build/bin/l4lb --check-config configs/example.conf
ctest --test-dir build -LE udp_long --output-on-failure
cmake -S . -B build-release -G Ninja -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Release
cmake --build build-release -j4
ctest --test-dir build-release --output-on-failure
cmake -S . -B build-production -G Ninja -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF
cmake --build build-production -j4
# 在普通UID运行，不用namespace root冒充权限检查
ctest --test-dir build -R '^cli_integration$' --output-on-failure
python3 tests/docs_links.py .
```

[README TCP原命令](../../README.md#tcp-最短运行路径)启动echo9001/9002、原example配置和Bash客户端，预期精确`hello S2`；产品消费SIGINT退出0，echo按POSIX信号退出。UDP及自动产品路径：

```bash
./build/bin/tcp_product_smoke ./build/bin/l4lb ./build/bin/tcp_echo_backend ./build/product-evidence
bash tests/udp_manual_demo.sh ./build/bin/l4lb ./build/udp-manual-evidence 18080
python3 tests/benchmark_runner.py --program ./build-production/bin/l4lb --protocol tcp --mode paired --warmup 0 --duration 1 --repeats 1 --rate 100 --output .stage-tmp/release-check/tcp-smoke
python3 tests/benchmark_runner.py --program ./build-production/bin/l4lb --protocol udp --mode paired --warmup 0 --duration 1 --repeats 1 --rate 100 --output .stage-tmp/release-check/udp-smoke
python3 tests/v04_benchmark_compare.py recompute --package docs/benchmarks/reports/v0.4-user-space.raw.json.gz --output .stage-tmp/release-check/recomputed
```

recomputed/summary.json应与[公开机器摘要](../benchmarks/reports/v0.4-user-space.json)完整JSON对象相同；不要覆盖原摘要或[正式报告](../benchmarks/reports/v0.4-user-space.md)。短smoke只是当前工具可用性，不重新测量正式48run，不形成新性能结论。长expiry至少一次；无实现变化则不新增sanitizer/重复负向框架。

## Builder与Reviewer独立结果

2026-09-10 Builder使用新106文件公开快照，重新执行上述参数，未复用S2的build或输出：

- Debug/Release/Production三构建成功；实际CTest注册31项，Debug快速 **30/30（113.44秒）**、Release完整 **31/31（167.56秒，含原60秒expiry）**，退出均0。
- 普通UID1000 CLI **44项通过**；help、TCP静态check、原README TCP四终端等效命令精确回显`hello S2`，原自动TCP验证和UDP单shell（含静态check）通过。TCP产品SIGINT退出0、echo按信号退出；UDP所有自有进程退出0，三/六端口分别重绑，PID/fd回收通过。
- 两套回归结束后串行执行当前Production TCP/UDP短paired，**各2run valid、退出0**；每run无primary/cleanup错误、自有进程reaped、fixture/backend fd回基线。历史包recompute与原公开完整JSON相等；不改变原性能数据或产生新性能主张。
- 106文件最终manifest与工作树一致；src/configs/正式reports的34保护文件与S2基线字节相同。现有离线链接检查通过；最终检查数量由收尾记录核对，文档纯结果更新不影响上述动态输入身份。

Reviewer于2026-09-10从另一份106文件公开快照独立验证，未复用Builder构建或输出，结论PASS：

- Debug快速 **30/30（118.58秒）**、Release完整 **31/31（166.31秒，含原长expiry）**、三构建及普通UID1000 CLI44全部通过。
- README原TCP argv/Bash客户端与UDP公开命令、自动TCP验收通过；当前TCP/UDP短paired各2run valid，完整历史JSON重算一致。自有PID/fd/端口回收，所有run无primary/cleanup错误。
- 27公开文档134本地链接/锚点通过；106文件manifest、72运行输入和34保护文件与工作区一致，生产/配置及正式数据相对S2保持。

Leader核对六标准和八项债务/观察处置后完成状态同步。收尾只改六份公开文档的状态/结果，动态身份保持，随后重新检查最终链接；不将文字收尾冒充新动态运行。完整角色manifest和过程日志留内部审计，公开复核使用本页命令即可。实际发布未执行。

## 六标准和债项处置

- README最短路径、TCP/UDP全回归、六类规格与实现一致性：以本轮结果和[六标准索引](v1.0-acceptance.md)核验。
- 限制与债：下列逐项处置记入[技术债跟踪](../../TECH-DEBT-TRACKER.md)，不新增未授权风险或掩盖阻塞。
- CHANGELOG版本变化：S1/S2及S3最终条目已经Leader在独立审查后填入[CHANGELOG](../../CHANGELOG.md)。
- 角色报告：S3 Builder001、Reviewer001独立PASS与Leader002收尾齐备；本地治理记录不随clone分发。

| 条目 | 本轮处置及影响 | 责任、目标、退出或再次触发条件 |
| --- | --- | --- |
| TD-001 | 已解决保持关闭；本轮重新执行真实README构建/示例确认 | Builder执行、Reviewer验收；今后命令长期不可运行则重新评估 |
| TD-002 | 已解决保持关闭；key=value与现有CTest/CLI继续验证，无新依赖 | Builder维护、Reviewer验收；配置/测试框架变化需兼容性评估 |
| TD-003 | 持续跟踪，不阻塞V1.0；不选择云实例、不承诺采购成本 | Leader组织，Builder准备、Reviewer验收；进入V1.1前确认Linux环境/权限/attach方式和复现证据，V1.2按实际数据面再验 |
| TD-004 | 用户态规格/运行/benchmark/版本矩阵已交付，本轮发布审查义务已独立验收并收尾；未来XDP义务保留，不整体关闭 | Leader按V1.1/V1.2设计安排Builder公开xdp-map-schema/runbook，Reviewer核对实现一致并独立验收后关闭对应义务 |
| OBS-001 | 文档/实际命令已有证据，当前不升级为债 | Leader/Builder保持同步，Reviewer核对；再次出现长期文档与实际不符时升级 |
| OBS-002 | 保留XDP与完整TCP proxy边界观察 | Leader设计把关、Reviewer验收；未来阶段将完整TCP代理塞入XDP时要求返工或新债 |
| OBS-003 | 有正式可重算性能包，本轮只验smoke和离线完整JSON；不扩大WSL结论 | 新平台/负载或新性能主张触发Leader安排匹配测量与Reviewer验收；缺少可复现数据时升级 |
| OBS-004 | TCP/UDP flow、调度、健康和control边界已经落地；未来XDP按路线推进 | Leader维护分层，Builder实现、Reviewer核对；退化成通用epoll或混入HTTP语义时重新评估，不把未来未实现本身当当前债 |

## 保持的限制

[稳定契约](v1.0-user-visible-contract.md)保持：六键静态IPv4、无失败换后端重试、Unknown初态/2成功3失败、无Healthy拒绝新业务而旧绑定不迁移、schema1混合stderr。TCP只在固定1秒内尝试用户态pending，UDP停止关闭flows；同步慢输出、SIGPIPE、强杀可能无尾快照，不保证送达或绝对退出时限。1024容量满载、公网relay安全、native XDP/物理网卡极限均不在本轮承诺内。
