# V0.2 完成标准与证据矩阵

本文角色报告/设计路径为本地治理记录（或未来计划位置），不随普通clone分发；公开复核使用README和规格/运行手册。

本矩阵对应 ROADMAP 的六条 V0.2 完成标准。S3与V0.2开发范围已完成（Completed，2026-09-09）：Builder001、Reviewer001独立PASS，Leader003核对六条标准并收尾；S3已合并，远端main与v0.2-s3标签peeled已核验为7821b25；记录时codex/v0.3-s1处于Draft准备，不据此声称创建GitHub Release。普通 clone 可通过公共源码、CTest和手册复现第1..5条，不需要本地忽略的角色报告。

| ROADMAP 标准 | 实现与验证层 | 普通 clone 复现入口 | 边界 |
|---|---|---|---|
| 1. TCP 路径保持可用 | 继承V0.1及S1/S2；S3继续全部真实TCP与状态回归 | `ctest --test-dir build -R 's2_|s3_product_smoke'` | TCP生产源码未改；二进制、半关闭、背压、错误和资源验证，不是性能结论 |
| 2. UDP datagram 可转发并收到后端响应 | S2真实0/1/NUL/65507、双向截断、内部错误注入；S3新增真实产品P1/P2 | `ctest --test-dir build -R 'v02_udp_(state|product|system)$'`；[完整手动流程](../runbooks/local-udp-validation.md) | wildcard两目的、按flow A/B/A、伪造过滤、同后端不同client反序回包和停机后新flow恢复有真实证据；注入不称真实网络 |
| 3. UDP flow 有明确超时行为 | S2受控时钟等于到期、零长刷新、丢弃不刷新、真实缩时热流量；S3 P3未改产品真实静默≥60.5秒、同key A→B | `ctest --test-dir build-release -R '^v02_udp_expiry$'`；`v02_udp_state` | P3仅要求最终Release一次；60秒为idle而非应用事务隔离。1024默认值静态确认，capacity2满表/旧flow可用/不推进由内部动态覆盖，未做1024满载产品验收 |
| 4. 调度策略可独立单测 | 继承S1 Scheduler接口、round-robin N=1/3/256、实例隔离/错误；S2/S3验证产品按flow不按包推进 | `ctest --test-dir build -R '^v02_scheduler_unit$'`，配合UDP state/product/system | 只有round_robin；资源失败仍消耗一次，容量/坏元数据不消耗；没有健康探测/权重/失败切换 |
| 5. 配置schema和UDP语义已更新 | S1兼容配置；S2完整数据面契约；S3完整公共系统/手动流程与证据分层 | [配置规格](config-schema.md)、[调度规格](scheduler.md)、[UDP flow](udp-flow-table.md)、[手册](../runbooks/local-udp-validation.md)；`config_unit`/`cli_integration` | 没有新配置字段、超时测试开关、可靠队列或公网安全承诺；普通权限项须在原uid补测 |
| 6. Builder和Reviewer报告已生成 | S3 Builder001和Reviewer001已生成，Reviewer独立PASS；Leader003完成六条标准核对及状态同步 | 可选本地报告 `docs/builder/reports/V0.2/S3-report-001.md`、`docs/reviewer/reports/V0.2/S3-report-001.md`、`docs/leader/reports/V0.2/S3-report-003.md` | 私有协作文档按既有gitignore管理，普通clone不要求公开发布这些报告；独立验收和Leader收尾门槛已满足，不代表发布 |

V0.2/S3验收时总注册11项，Debug快速执行10项（排除udp_long），Release全套11项含P3。`udp_fast`两项各重复三次、Release两组并行/空格路径及四类工具可靠性负向属于S3证据，不替代第1..5条真实数据断言。标准命令和独立namespace路线均见运行手册。

工具可靠性负向须在临时副本中到达目标分支：错误回复源/归属期望、后端替身提前非零退出、缺完整ready、cleanup失败；必须非零、内部截止与清理传播，不能用配置错误或外层124代替。S2既有payload/零长/选择负向保留，按实际受影响模块复验。

不属于本版本验收：1024真实饱和、吞吐/延迟指标、公网可达性或防护、UDP重传/可靠性、IPv6、多策略、健康检查、XDP、发布标签。测试/文档完成不自动授权提交、推送、合并或发布。
