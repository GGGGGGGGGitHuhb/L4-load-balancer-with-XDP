# V0.1 验收矩阵

本文角色报告/设计路径为本地治理记录（或未来计划位置），不随普通clone分发；公开复核使用README和规格/运行手册。

历史验收日期：2026-09-08。以下提交/发布及测试数量描述当时状态，当前入口见[README](../../README.md)。状态：S3 Completed，独立审查 PASS，Leader 已完成七条标准核对；V0.1 开发范围完成，S3 尚未提交、合并或发布。此文档映射 ROADMAP 的七条 V0.1 完成标准，不另行定义 TCP 契约，也不表示已合并或发布。

## 完成标准与证据入口

1. 用户可以按 README 构建：Debug/Release 独立配置和构建退出 0；生产 BUILD_TESTING=OFF 构建退出 0，bin 仅含 l4lb。复现见[运行手册](../runbooks/local-tcp-validation.md#构建与自动验收)。
2. 用户可以启动本地后端和负载均衡器：README 固定端口四终端路径实跑，客户端返回 hello S2；P1/P3 使用动态端口及完整 ready 屏障。
3. TCP 客户端经过负载均衡器到后端：P1 四次二进制完整 echo 与 A/B/A/B 日志；P2 1048593 字节 SHUT_WR 后逐字节一致并 EOF；P3 停机新连接失败、代理存活、同端口恢复后的新连接成功。转发能力继承 S2，本阶段新增真实产品自动化证据。
4. 配置错误给出明确失败：继承 config_unit/cli_integration，新增 P4 真实 `--run` 坏配置退出 1、无 ready、有诊断；占用端口明确 bind 失败。
5. 单元和 TCP 集成测试通过：Debug/Release 全套各 6/6（config_unit、cli_integration、s2_net_unit、s2_tcp_integration、s2_reactor_state、s3_product_smoke）；s3 各连续 3 次、双实例并行通过。错误数据/提前退出/无 ready 负向副本两模式各退出 1，不接受外层超时当作通过。S2 原反向 FIN 后端断言结果传播保留。
6. README 运行、测试、构建命令同步：保留手动路径，新增自动入口、证据及失败诊断、生产无测试构建；详细命令见跟踪内运行手册。普通 clone 不需要本地角色文档。
7. Builder 和 Reviewer 报告生成：Builder `docs/builder/reports/V0.1/S3-report-001.md` 在本地协作目录生成；Reviewer `docs/reviewer/reports/V0.1/S3-report-001.md` 已生成且 PASS，Leader `docs/leader/reports/V0.1/S3-report-003.md` 已完成收尾。这些协作报告默认忽略，不作为普通使用者运行前提；七条标准均经独立证据确认，本矩阵与当前状态已同步。

## S3 证据边界

- 新增：测试直接 exec 真正的 l4lb 和 echo，不包含/链接生产 reactor 内部，也不使用 tcp_test_server 注入；mkdtemp 隔离配置和输出、就绪截止、交错 I/O、回收状态、端口重绑和失败诊断。
- 继承：S1 配置/CLI；S2 round-robin、背压、双向关闭、超时、信号停止、错误隔离；精细状态路径仍由 S2 测试验证，不用单一 echo 替代。
- 实跑环境、退出码和证据根见[手册验证记录](../runbooks/local-tcp-validation.md#验证记录与范围)；本地完整日志为 `.stage-tmp/v0.1-s3/builder/` 和独立 `.stage-tmp/v0.1-s3/reviewer/`，后者 final-check.txt、evidence-summary.txt、probe-summary.txt 保存验收摘要。普通 clone 可按手册重新生成独立结果。
- 没有修改生产源码、配置格式或 5 秒连接/60 秒空闲超时；示例配置 SHA256 前后相同。不做 UDP、健康检查、性能承诺或 XDP。
- 历史 S2 首轮 FAIL 与后续修复/复审报告保留。独立 Reviewer 已通过且无阻塞，Leader 已关闭 V0.1 开发范围；PR、合并及标签需要另外授权。
